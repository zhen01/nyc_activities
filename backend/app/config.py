"""
App configuration loaded from environment variables (see .env.example).

Holds settings like DATABASE_URL, ALLOWED_ORIGINS, and feature flags
(e.g. USE_LLM_EXPLANATIONS) so behavior can change without code edits.
"""

# TODO: define a Settings class (pydantic-settings BaseSettings) once
# dependencies are installed.
