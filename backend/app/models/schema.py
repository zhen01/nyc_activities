"""
Pydantic request/response models shared between the API layer and services.

UserConstraints: what the frontend sends — time window, budget, max travel
    time/location, solo vs. group, skill level.
Recommendation: what the API returns — activity fields, an explanation of
    why it fits, and an explicit list of uncertainties (principle #4).
"""

# TODO: define UserConstraints, Activity, and Recommendation models.
