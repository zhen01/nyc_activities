services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-nyc_activities}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nyc_activities}
      POSTGRES_DB: ${POSTGRES_DB:-nyc_activities}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nyc_activities}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
