/**
 * Collects the user's feasibility constraints: available time window,
 * budget ceiling, max travel time/location, solo vs. group, skill level.
 * This maps 1:1 to the backend's UserConstraints model.
 */

// TODO: build controlled form + submit handler.
