/**
 * Renders one Recommendation: activity details, the "why this fits"
 * explanation, and any explicit uncertainty notes (e.g. stale source).
 * Uncertainty must be shown, not hidden -- principle #4.
 */

// TODO: render activity fields + explanation + uncertainty badge.
