{
  "name": "nyc-activities-frontend",
  "private": true,
  "version": "0.0.1",
  "description": "Minimal React frontend: one form (ConstraintForm) and a small results list (ActivityCard). No routing, no auth, no state library. Talks to the backend's single /recommendations endpoint via src/api/client.ts.",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.2.0"
  }
}
